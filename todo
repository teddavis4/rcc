show the old score (if any) when changing the score
    a list you can download (What format? I'll show you the raw text file
    in a followup email)
    show games in a table, vote for multiple games (?vote)
    fix duplicate users
